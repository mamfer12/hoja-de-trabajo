{
  "name": "beca-academica-ts-jest",
  "version": "1.0.0",
  "private": true,
  "description": "Evaluación de beca académica con clases de equivalencia, valores frontera y tabla de decisión. Tests con Jest + ts-jest.",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3",
    "rimraf": "^6.0.1"
  }
}
